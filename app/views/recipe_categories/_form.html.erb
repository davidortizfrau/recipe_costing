<%= form_for(@recipe_category) do |f| %>
  <% if @recipe_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe_category.errors.count, "error") %> prohibited this recipe_category from being saved:</h2>

      <ul>
      <% @recipe_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br />
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="form-group">
    <% if @recipe_category.name %>
      <%= f.submit "Edit Category", class: "btn btn-default" %>
    <% else %>
      <%= f.submit "Add Category", class: "btn btn-default" %>
    <% end %>
  </div>
<% end %>
