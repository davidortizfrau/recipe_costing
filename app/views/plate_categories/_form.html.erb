<%= form_for(@plate_category) do |f| %>
  <% if @plate_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@plate_category.errors.count, "error") %> prohibited this plate_category from being saved:</h2>

      <ul>
      <% @plate_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br />
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="form-group">
    <% if @plate_category.name %>
      <%= f.submit "Edit Category", class: "btn btn-default" %>
    <% else %>
      <%= f.submit "Add Category", class: "btn btn-default" %>
    <% end %>
  </div>
<% end %>
