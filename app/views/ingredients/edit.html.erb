<h1 class="page-header">Editing <%= @ingredient.name %>
 <%= link_to 'Delete', @ingredient, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger btn-xs" %>
</h1>

<div class="row">
	<div class="col-md-4">
		<%= render 'form' %>
	</div>
</div>